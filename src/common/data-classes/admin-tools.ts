export interface AdminContent {
  itemName: string,
  label?: string,
  type: string
}

export interface NeodeBatchQueryItem {
  query: string,
  params: object
}

export interface NoteItem {
  name: string
}
